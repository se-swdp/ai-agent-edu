# AI 전파교육 대시보드
.meta-loop/
# .claude 는 로컬 세션 파일이라 제외하되, 프로젝트 스킬(발표자료 제작 가이드 등)은 공유·푸시한다
.claude/*
!.claude/skills/
.DS_Store
Thumbs.db
node_modules/
*.log
.env

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
*-debug.log
.runtimeconfig.json

# Firebase service account secret keys — NEVER commit these
*-firebase-adminsdk-*.json
serviceAccountKey*.json

# Large media (>100MB) — kept on Firebase Hosting only, not in git
# (GitHub의 100MB 제한 회피. 운영용은 .mp4 변환본 사용)
*.MOV
*.mov


# Note-deck source PNGs (originals kept local; webp is the tracked/deployed form)
**/src-png/
**/.verify-shots/

# playwright-cli local caches
.playwright-cli/
**/.playwright-cli/
